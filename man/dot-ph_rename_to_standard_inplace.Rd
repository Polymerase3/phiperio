% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert-standard.R
\name{.ph_rename_to_standard_inplace}
\alias{.ph_rename_to_standard_inplace}
\title{Rename columns to PHIPERIO standard names in-place}
\usage{
.ph_rename_to_standard_inplace(tbl, con, colname_map)
}
\arguments{
\item{tbl}{Character scalar. Name of the DuckDB table or view to modify.}

\item{con}{A valid DBI connection to DuckDB.}

\item{colname_map}{Named character list mapping \strong{standard} PHIPERIO column
names (e.g. \code{"sample_id"}, \code{"peptide_id"}) to the \strong{actual} column names
present in \code{tbl}.}
}
\value{
Invisibly returns \code{tbl} after applying any renames.
}
\description{
\code{.ph_rename_to_standard_inplace()} renames columns in a DuckDB
table or view to the standard PHIPERIO schema using a mapping of standard
names to source columns. For views, it recreates the view with aliased
columns; for tables, it issues \verb{ALTER TABLE ... RENAME COLUMN} statements.
}
\details{
\itemize{
\item Only columns present in \code{tbl} are renamed.
\item For views, the existing definition is retrieved and wrapped in a new
\verb{CREATE OR REPLACE VIEW} statement with aliased columns.
\item If no matching columns are found, the function emits a message and exits.
}
}
\keyword{internal}
