# Refresh finalizer connection list

Updates the finalizer environment with the current connections.

## Usage

``` r
.ph_refresh_finalizer(x)
```

## Arguments

- x:

  A valid `phip_data` object.

## Value

A `phip_data` object with refreshed finalizer connections.
