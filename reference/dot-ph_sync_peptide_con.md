# Sync peptide connection handle

Ensures `meta$peptide_con` mirrors the peptide library attribute.

## Usage

``` r
.ph_sync_peptide_con(x)
```

## Arguments

- x:

  A valid `phip_data` object.

## Value

A `phip_data` object with `meta$peptide_con` updated.
